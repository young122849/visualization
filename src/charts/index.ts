export interface ProtoConfig {
  target?: string,
  width?: string,
  height?: string,
  margin?: any
}

export const config = {
  target: 'body',
  width: '100%',
  height: '100%',
  margin: {
    top: 20,
    right: 20,
    bottom: 20,
    left: 20
  }
}